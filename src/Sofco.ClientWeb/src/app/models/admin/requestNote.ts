export interface RequestNote {

}

export interface SearchFilters {
    id?: number,
    creationUserId?: number,
    fromDate?: string,
    toDate?: string,
    providerId?: number,
    statusId?: number
}